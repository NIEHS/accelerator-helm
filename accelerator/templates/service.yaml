apiVersion: v1
kind: Service
metadata:
  name: {{ include "accelerator.fullname" . }}
  labels:
    {{- include "accelerator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "accelerator.selectorLabels" . | nindent 4 }}
