apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "accelerator.fullname" . }}-test-connection"
  labels:
    {{- include "accelerator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "accelerator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
